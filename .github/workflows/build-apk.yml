name: Build Jane Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Unpack Jane package
        run: |
          rm -rf source unpacked
          mkdir -p unpacked

          if [ -f JaneAIAndroidSource_static_glb_home_v46_aggressive_skin_clean.zip ]; then
            unzip JaneAIAndroidSource_static_glb_home_v46_aggressive_skin_clean.zip -d source
          elif [ -f JaneStaticGLBHomeV46AggressiveSkinCleanPackage.zip ]; then
            unzip JaneStaticGLBHomeV46AggressiveSkinCleanPackage.zip -d unpacked
            unzip unpacked/JaneAIAndroidSource_static_glb_home_v46_aggressive_skin_clean.zip -d source
          else
            ls -la
            exit 1
          fi

      - name: Verify V46 retained features and GLB
        working-directory: source
        run: |
          grep -q "applyV34DialogScrollFix" app/src/main/assets/index.html
          grep -q "Permanent imported knowledge library" app/src/main/assets/index.html
          grep -q "Launch Jane" app/src/main/assets/index.html
          grep -q "V46: same latest accepted app features" app/src/main/assets/index.html
          ! grep -q "janeSpeechWatchdog" app/src/main/assets/index.html
          sha=$(sha256sum app/src/main/assets/jane_static_model_mobile_small.glb | awk '{print $1}')
          [ "$sha" = "cae8bc1c6e972532340e1cb920dab7a6c8638a2b4f972ad41fe28911515fac2b" ]

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        working-directory: source
        run: gradle :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: JaneAI-debug-apk
          path: source/app/build/outputs/apk/debug/app-debug.apk
