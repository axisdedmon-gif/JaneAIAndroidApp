name: Build Jane Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Unpack Jane package
        run: |
          rm -rf source unpacked
          mkdir -p unpacked

          if [ -f JaneAIAndroidSource_static_glb_home_v43_user_glb_latest.zip ]; then
            unzip JaneAIAndroidSource_static_glb_home_v43_user_glb_latest.zip -d source
          elif [ -f JaneStaticGLBHomeV43LatestUserGlbPackage.zip ]; then
            unzip JaneStaticGLBHomeV43LatestUserGlbPackage.zip -d unpacked
            unzip unpacked/JaneAIAndroidSource_static_glb_home_v43_user_glb_latest.zip -d source
          else
            ls -la
            exit 1
          fi

      - name: Verify retained features and GLB
        working-directory: source
        run: |
          grep -q "applyV34DialogScrollFix" app/src/main/assets/index.html
          grep -q "Permanent imported knowledge library" app/src/main/assets/index.html
          grep -q "Launch Jane" app/src/main/assets/index.html
          sha=$(sha256sum app/src/main/assets/jane_static_model_mobile_small.glb | awk '{print $1}')
          [ "$sha" = "0dca686d2f8ae6a8676446f24856ae7491ea91931eeb5e5d9806951304931604" ]

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        working-directory: source
        run: gradle :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: JaneAI-debug-apk
          path: source/app/build/outputs/apk/debug/app-debug.apk
