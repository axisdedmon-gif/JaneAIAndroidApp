name: Build Jane Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Unpack Jane V69 package
        run: |
          rm -rf source unpacked
          mkdir -p unpacked

          if [ -f JaneAIAndroidSource_v69_active_vitals_icon_face_center.zip ]; then
            unzip JaneAIAndroidSource_v69_active_vitals_icon_face_center.zip -d source
          elif [ -f JaneV69ActiveVitalsIconFaceCenterPackage.zip ]; then
            unzip JaneV69ActiveVitalsIconFaceCenterPackage.zip -d unpacked
            unzip unpacked/JaneAIAndroidSource_v69_active_vitals_icon_face_center.zip -d source
          else
            echo "Package missing."
            ls -la
            exit 1
          fi

      - name: Verify V69 active vitals and launcher icon
        working-directory: source
        run: |
          echo "=== verify source structure ==="
          test -f app/src/main/assets/index.html
          test -f app/src/main/java/com/example/janeai/MainActivity.java

          echo "=== verify launcher icon WebP assets exist ==="
          test -f app/src/main/res/mipmap-mdpi/ic_launcher.webp
          test -f app/src/main/res/mipmap-hdpi/ic_launcher.webp
          test -f app/src/main/res/mipmap-xhdpi/ic_launcher.webp
          test -f app/src/main/res/mipmap-xxhdpi/ic_launcher.webp
          test -f app/src/main/res/mipmap-xxxhdpi/ic_launcher.webp
          test -f app/src/main/res/mipmap-mdpi/ic_launcher_round.webp
          test -f app/src/main/res/mipmap-hdpi/ic_launcher_round.webp
          test -f app/src/main/res/mipmap-xhdpi/ic_launcher_round.webp
          test -f app/src/main/res/mipmap-xxhdpi/ic_launcher_round.webp
          test -f app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.webp
          test -f app/src/main/res/drawable/ic_launcher_foreground.webp
          test -f app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
          test -f app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml

          echo "=== verify duplicate launcher PNG files are gone ==="
          ! find app/src/main/res -path "*mipmap-*/ic_launcher.png" | grep -q .
          ! find app/src/main/res -path "*mipmap-*/ic_launcher_round.png" | grep -q .
          ! find app/src/main/res -path "*drawable/ic_launcher_foreground.png" | grep -q .

          echo "=== verify active 4-per-side vitals HUD ==="
          grep -q "v69-active-vitals-hud-css" app/src/main/assets/index.html
          grep -q "v69-active-vitals-hud-overlay" app/src/main/assets/index.html
          grep -q "janeVitalsHUD" app/src/main/assets/index.html
          grep -q "72 BPM" app/src/main/assets/index.html
          grep -q "99%" app/src/main/assets/index.html
          grep -q "Calm" app/src/main/assets/index.html
          grep -q "68%" app/src/main/assets/index.html
          grep -q "98.6°F" app/src/main/assets/index.html
          grep -q "Online" app/src/main/assets/index.html
          grep -q "Strong" app/src/main/assets/index.html
          grep -q "82%" app/src/main/assets/index.html
          grep -q "v69WaveSlide" app/src/main/assets/index.html

          echo "=== verify retained features and unchanged GLB baseline ==="
          grep -q "data:model/gltf-binary;base64," app/src/main/assets/index.html
          grep -q "v58-offline-knowledge-engine" app/src/main/assets/index.html
          grep -q "v67b-transparent-webp-dialog-portrait-router" app/src/main/assets/index.html
          grep -q "v66-model-up-only-css" app/src/main/assets/index.html
          grep -q "v65-clean-tools-engine" app/src/main/assets/index.html
          grep -q "rotation-per-second" app/src/main/assets/index.html
          grep -q "36deg" app/src/main/assets/index.html
          grep -q "Deathcore101!!" app/src/main/assets/index.html
          grep -q "playAssetVoice" app/src/main/java/com/example/janeai/MainActivity.java

          echo "=== verify broken/dead pieces are gone ==="
          ! grep -R "v68-vitals-hud-css\|v68-vitals-hud-overlay" app/src/main
          ! grep -R "https://js.puter.com/v2/\|gemini-3.1-flash-lite-preview\|gemini-2.5-flash-image-preview" app/src/main
          ! grep -R "BiometricPrompt\|USE_BIOMETRIC\|USE_FINGERPRINT\|runOwnerBiometricCheck\|notifyOwnerCheckResult\|requestOwnerCheck\|Waiting for biometric check" app/src/main
          ! grep -R "jane_static_model_mobile_small.glb\|Meshy_AI_Kadi\|Meshy_AI_Midnight_Dragon_0512021503_texture.glb" app/src/main

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        working-directory: source
        run: gradle :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: JaneAI-debug-apk
          path: source/app/build/outputs/apk/debug/app-debug.apk
