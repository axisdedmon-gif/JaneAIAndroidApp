--- v78work/app/src/main/assets/index.html	2026-08-04 22:18:06.000000000 +0000
+++ v78mod/app/src/main/assets/index.html	2026-08-05 00:25:13.680040628 +0000
@@ -1365,35 +1365,47 @@
 
   let localKnowledgeContext="";
   let localKnowledgeFallback=null;
+  let knowledgeStats={docs:0,chunks:0,names:[]};
   try{
     if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.ready==="function") await window.JaneOfflineKnowledge.ready();
-    if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.buildContext==="function"){
-      localKnowledgeContext=await window.JaneOfflineKnowledge.buildContext(message,12000);
-    }
-    if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.offlineAnswer==="function"){
-      localKnowledgeFallback=await window.JaneOfflineKnowledge.offlineAnswer(message);
-    }
+    if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.stats==="function") knowledgeStats=await window.JaneOfflineKnowledge.stats();
+    if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.buildContext==="function") localKnowledgeContext=await window.JaneOfflineKnowledge.buildContext(message,14000);
+    if(window.JaneOfflineKnowledge && typeof window.JaneOfflineKnowledge.offlineAnswer==="function") localKnowledgeFallback=await window.JaneOfflineKnowledge.offlineAnswer(message);
   }catch(knowledgeError){
-    console.warn("[Jane V76 Knowledge context]",knowledgeError);
+    console.warn("[Jane V78 Knowledge context]",knowledgeError);
   }
 
-  const knowledgeRule=localKnowledgeContext
-    ? "\n\nUPLOADED OFFLINE KNOWLEDGE IS THE PRIMARY SOURCE FOR THIS QUESTION. Use the excerpts below before general model knowledge. When the excerpts cover the subject, answer from them and name the source files. If the uploaded library does not contain enough information, say so instead of inventing details.\n\n"+localKnowledgeContext
-    : "\n\nNo strongly matching excerpt was found in Jane's uploaded offline knowledge library. Do not claim that an uploaded file supports the answer unless a source excerpt is provided.\n";
+  if(!knowledgeStats.docs){
+    return "I don’t know that yet. My Knowledge Base is empty, so you’ll need to give me a PDF, file, photo, or document that teaches me about it.";
+  }
+  if(!localKnowledgeContext){
+    return "I don’t know that from the knowledge you’ve given me. Add a source that covers it, or ask me something supported by the Archives.";
+  }
+
+  const knowledgeRule=`
+JANE KNOWLEDGE-BOUND MODE — MANDATORY:
+1. The SOURCE EXCERPTS below are the only factual knowledge you may use.
+2. Do not use general training knowledge, web knowledge, memory outside these excerpts, or unstated conversion facts.
+3. You may reason, compare, summarize, combine passages, and perform arithmetic or logical deductions only when every needed fact or conversion factor is present in the excerpts.
+4. If the excerpts mention the subject but do not contain enough facts to support the requested conclusion, answer exactly that you do not know from the knowledge C.J. has given you.
+5. Answer naturally and directly. End with a brief “Sources:” line naming only the supplied files actually used.
+
+SOURCE EXCERPTS:
+${localKnowledgeContext}`;
 
-  const body={message:privateContextText()+"\n\n"+identityTone+knowledgeRule+"\nUser message:\n"+message,history:state.history};
+  const body={message:privateContextText()+"\n\n"+identityTone+knowledgeRule+"\n\nUser question:\n"+message,history:state.history};
   if(state.attachedFile){body.fileName=state.attachedFile.name;body.fileMimeType=state.attachedFile.mimeType;body.fileBase64=state.attachedFile.base64;}
 
-  if(navigator.onLine===false && localKnowledgeFallback) return localKnowledgeFallback;
+  if(navigator.onLine===false) return localKnowledgeFallback||"I found relevant material in the Archives, but I need the reasoning service online to compile the answer.";
 
   try{
     const res=await fetch(CHAT_ENDPOINT,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});
@@ -1401,7 +1413,7 @@
     const raw=await res.text();
     let data;try{data=JSON.parse(raw)}catch{data={error:raw}}
     if(!res.ok)throw new Error(data.error||"Chat failed");
-    return data.reply||localKnowledgeFallback||"I’m here, but I do not have a reply ready.";
+    return data.reply||localKnowledgeFallback||"I don’t know that from the knowledge you’ve given me.";
   }catch(error){
     if(localKnowledgeFallback) return localKnowledgeFallback;
     throw error;
@@ -2410,6 +2422,7 @@
     await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});
     cache.docs.push(doc);cache.chunks.push(...chunkRows);updateStatus();
+    document.dispatchEvent(new CustomEvent("jane-knowledge-changed",{detail:{action:"import",docId:doc.id}}));
     return {duplicate:false,doc};
   }
   function searchKnowledge(query,limit){
@@ -2440,7 +2453,27 @@
       return (i+1)+". "+h.chunk.docName+"\n"+text;
     }).join("\n\n");
   }
-  async function clear(){const db=await openDb();await Promise.all(["docs","chunks"].map(name=>new Promise((resolve,reject)=>{const tx=db.transaction(name,"readwrite");tx.objectStore(name).clear();tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);})));cache.docs=[];cache.chunks=[];updateStatus();}
+  async function listDocuments(){
+    await ready();
+    return cache.docs.slice().sort((a,b)=>String(b.importedAt||"").localeCompare(String(a.importedAt||"")));
+  }
+  async function getDocument(docId){
+    await ready();
+    const doc=cache.docs.find(d=>d.id===docId);if(!doc)return null;
+    const chunks=cache.chunks.filter(c=>c.docId===docId).sort((a,b)=>a.idx-b.idx);
+    return {doc,chunks,text:chunks.map(c=>c.text).join("\n\n")};
+  }
+  async function deleteDocument(docId){
+    await ready();
+    const db=await openDb();
+    const ids=cache.chunks.filter(c=>c.docId===docId).map(c=>c.id);
+    const tx=db.transaction(["docs","chunks"],"readwrite");
+    tx.objectStore("docs").delete(docId);ids.forEach(id=>tx.objectStore("chunks").delete(id));
+    await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});
+    cache.docs=cache.docs.filter(d=>d.id!==docId);cache.chunks=cache.chunks.filter(c=>c.docId!==docId);updateStatus();
+    document.dispatchEvent(new CustomEvent("jane-knowledge-changed",{detail:{action:"delete",docId}}));
+    return true;
+  }
+  async function clear(){const db=await openDb();await Promise.all(["docs","chunks"].map(name=>new Promise((resolve,reject)=>{const tx=db.transaction(name,"readwrite");tx.objectStore(name).clear();tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);})));cache.docs=[];cache.chunks=[];updateStatus();document.dispatchEvent(new CustomEvent("jane-knowledge-changed",{detail:{action:"clear"}}));}
   function updateStatus(message){
     const el=document.getElementById("knowledgeStatus");
     if(el)el.textContent=message||((cache.docs.length?cache.docs.length+" offline knowledge file(s), "+cache.chunks.length+" searchable sections stored on this phone.":"No offline knowledge files imported yet."));
@@ -2477,7 +2510,7 @@
   };
   window.JaneKnowledgeImportError=function(id,name,message){nativeImports.delete(id);showProgress("Could not import "+name+": "+message);};
 
-  window.JaneOfflineKnowledge={ready,importKnowledge,searchKnowledge:async(q,l)=>{await ready();return searchKnowledge(q,l);},buildContext,offlineAnswer,stats:async()=>{await ready();return {docs:cache.docs.length,chunks:cache.chunks.length,names:cache.docs.map(d=>d.name)};},clear};
+  window.JaneOfflineKnowledge={ready,importKnowledge,searchKnowledge:async(q,l)=>{await ready();return searchKnowledge(q,l);},buildContext,offlineAnswer,listDocuments,getDocument,deleteDocument,stats:async()=>{await ready();return {docs:cache.docs.length,chunks:cache.chunks.length,names:cache.docs.map(d=>d.name)};},clear};
   ready().catch(e=>updateStatus("Offline knowledge database error: "+e.message));
 })();
 </script>
@@ -3265,6 +3298,69 @@
 </script>
 <!-- V77: tool menu cannot bleed across screens; owner phrase access restored; Offline Knowledge Library importer is visible in Private Settings. -->
 
+
+<style id="v78-knowledge-base-archives-css">
+#janeKnowledgeBase{position:fixed;inset:0;z-index:100300;display:none;overflow-y:auto;background:radial-gradient(circle at 50% 5%,rgba(23,86,115,.27),rgba(4,5,11,.98) 38%,#030308 100%);color:#f7f3ff;padding:18px 14px 38px;font-family:inherit}
+#janeKnowledgeBase.open{display:block}
+.v78-kb-shell{max-width:900px;margin:0 auto}
+.v78-kb-head{position:sticky;top:0;z-index:3;display:flex;justify-content:space-between;align-items:flex-start;gap:14px;padding:12px 4px 16px;background:linear-gradient(180deg,rgba(4,5,11,.98) 72%,rgba(4,5,11,0))}
+.v78-kb-head h2{margin:0;font-size:32px}.v78-kb-head p{margin:5px 0 0;color:rgba(238,232,249,.7);line-height:1.35}
+.v78-kb-actions{display:flex;flex-wrap:wrap;gap:9px}.v78-kb-btn{border:1px solid rgba(106,221,255,.27);border-radius:15px;background:linear-gradient(135deg,rgba(26,91,128,.72),rgba(76,57,122,.72));color:#fff;padding:11px 14px;font:inherit}.v78-kb-btn.secondary{background:rgba(255,255,255,.055)}.v78-kb-btn.danger{border-color:rgba(255,90,112,.3);background:rgba(91,22,38,.58)}
+.v78-kb-stats{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:4px 0 14px}.v78-kb-stat{border:1px solid rgba(99,224,255,.18);border-radius:18px;background:rgba(8,18,30,.72);padding:13px}.v78-kb-stat span{display:block;color:#8bddeb;font-size:12px;text-transform:uppercase;letter-spacing:.08em}.v78-kb-stat strong{display:block;margin-top:4px;font-size:20px}
+.v78-kb-toolbar{display:flex;gap:10px;margin-bottom:14px}.v78-kb-toolbar input{flex:1;min-width:0;border:1px solid rgba(255,255,255,.15);border-radius:16px;background:rgba(0,0,0,.34);color:#fff;padding:13px 14px;font:inherit;outline:none}
+#v78ArchiveList{display:grid;gap:11px}.v78-archive-card{display:grid;grid-template-columns:42px minmax(0,1fr) auto;gap:11px;align-items:center;border:1px solid rgba(112,222,255,.17);border-radius:19px;background:linear-gradient(145deg,rgba(13,25,38,.88),rgba(20,14,29,.82));padding:13px;box-shadow:0 12px 35px rgba(0,0,0,.18)}
+.v78-archive-icon{width:42px;height:42px;border-radius:13px;display:grid;place-items:center;background:rgba(76,205,238,.1);font-size:22px}.v78-archive-title{font-weight:800;overflow-wrap:anywhere}.v78-archive-meta{margin-top:4px;color:rgba(238,232,249,.63);font-size:12px;line-height:1.4}.v78-archive-buttons{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}.v78-archive-buttons button{padding:9px 11px;border-radius:13px;font-size:13px}
+.v78-kb-empty{border:1px dashed rgba(112,222,255,.2);border-radius:20px;padding:28px;text-align:center;color:rgba(238,232,249,.68);background:rgba(0,0,0,.2)}
+#v78ArchiveReader{display:none}.v78-reader-open #v78ArchiveBrowser{display:none}.v78-reader-open #v78ArchiveReader{display:block}.v78-reader-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:12px}.v78-reader-head h3{font-size:25px;margin:0;overflow-wrap:anywhere}.v78-reader-meta{color:rgba(238,232,249,.63);font-size:13px;margin-top:5px}.v78-reader-text{white-space:pre-wrap;overflow-wrap:anywhere;line-height:1.58;border:1px solid rgba(255,255,255,.12);border-radius:20px;background:rgba(0,0,0,.34);padding:17px;min-height:50vh;max-height:72vh;overflow:auto;font-family:inherit;color:#f4f0f7}
+.v78-knowledge-rule{border:1px solid rgba(255,203,93,.22);border-radius:18px;padding:13px;margin:0 0 14px;background:rgba(63,42,8,.25);color:#ffe4a2;line-height:1.45}
+body.v78-knowledge-open #janeToolsMenu{display:none!important;visibility:hidden!important;pointer-events:none!important}
+@media(max-width:560px){#janeKnowledgeBase{padding:10px 10px 28px}.v78-kb-head{align-items:stretch;flex-direction:column}.v78-kb-head h2{font-size:28px}.v78-kb-stats{grid-template-columns:1fr 1fr}.v78-kb-stat:last-child{grid-column:1/-1}.v78-archive-card{grid-template-columns:36px minmax(0,1fr)}.v78-archive-icon{width:36px;height:36px}.v78-archive-buttons{grid-column:1/-1;justify-content:flex-start}.v78-archive-buttons button{flex:1}.v78-kb-toolbar{flex-direction:column}}
+</style>
+<section id="janeKnowledgeBase" aria-hidden="true">
+  <div class="v78-kb-shell">
+    <header class="v78-kb-head">
+      <div><h2>Knowledge Base</h2><p>Jane’s learned intelligence and browsable Archives.</p></div>
+      <div class="v78-kb-actions"><button id="v78ImportBtn" class="v78-kb-btn" type="button">Import Knowledge</button><button id="v78CloseKnowledgeBtn" class="v78-kb-btn secondary" type="button">Close</button></div>
+    </header>
+    <div class="v78-knowledge-rule"><strong>Knowledge-only mode:</strong> Jane answers only from material stored here. She can combine, calculate, compare, and infer from these sources, but she will say she does not know when the required facts are absent.</div>
+    <div class="v78-kb-stats"><div class="v78-kb-stat"><span>Archive files</span><strong id="v78DocCount">0</strong></div><div class="v78-kb-stat"><span>Searchable sections</span><strong id="v78ChunkCount">0</strong></div><div class="v78-kb-stat"><span>Answer mode</span><strong>Knowledge only</strong></div></div>
+    <div id="v78ArchiveBrowser">
+      <div class="v78-kb-toolbar"><input id="v78ArchiveSearch" type="search" placeholder="Search uploaded titles and archive text…"><button id="v78RefreshArchives" class="v78-kb-btn secondary" type="button">Refresh</button></div>
+      <div id="v78ArchiveList"><div class="v78-kb-empty">Loading Archives…</div></div>
+    </div>
+    <div id="v78ArchiveReader">
+      <div class="v78-reader-head"><div><h3 id="v78ReaderTitle">Archive entry</h3><div id="v78ReaderMeta" class="v78-reader-meta"></div></div><button id="v78ReaderBack" class="v78-kb-btn secondary" type="button">Back to Archives</button></div>
+      <div id="v78ReaderText" class="v78-reader-text"></div>
+    </div>
+  </div>
+</section>
+<script id="v78-knowledge-base-archives-engine">
+(function(){
+  const byId=id=>document.getElementById(id);
+  let docs=[];
+  function closeTools(){const menu=byId("janeToolsMenu");if(menu){menu.classList.remove("open");menu.setAttribute("aria-hidden","true");menu.style.display="none";}}
+  function humanBytes(value){const n=Number(value||0);if(!n)return "size unavailable";const units=["B","KB","MB","GB"];let x=n,i=0;while(x>=1024&&i<units.length-1){x/=1024;i++;}return (i?x.toFixed(x>=10?1:2):Math.round(x))+" "+units[i];}
+  function dateText(value){try{return new Date(value).toLocaleString();}catch(e){return String(value||"");}}
+  function iconFor(mime,name){const t=String(mime||"").toLowerCase(),n=String(name||"").toLowerCase();if(t.includes("pdf")||n.endsWith(".pdf"))return "📕";if(t.startsWith("image/")||/\.(png|jpe?g|webp|gif|heic)$/.test(n))return "🖼️";if(n.endsWith(".docx")||t.includes("word"))return "📘";if(t.includes("json")||n.endsWith(".json"))return "🧩";return "📄";}
+  function escapeHtml(value){return String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));}
+  function openScreen(){closeTools();byId("ownerModal")?.classList.remove("open");const screen=byId("janeKnowledgeBase");screen?.classList.add("open");screen?.setAttribute("aria-hidden","false");document.body.classList.add("v78-knowledge-open");showBrowser();refresh();}
+  function closeScreen(){const screen=byId("janeKnowledgeBase");screen?.classList.remove("open","v78-reader-open");screen?.setAttribute("aria-hidden","true");document.body.classList.remove("v78-knowledge-open");}
+  function showBrowser(){byId("janeKnowledgeBase")?.classList.remove("v78-reader-open");}
+  async function refresh(){
+    const api=window.JaneOfflineKnowledge;if(!api)return;
+    await api.ready();docs=await api.listDocuments();const stats=await api.stats();byId("v78DocCount").textContent=stats.docs;byId("v78ChunkCount").textContent=stats.chunks;render();
+  }
+  async function render(){
+    const list=byId("v78ArchiveList");if(!list)return;const q=String(byId("v78ArchiveSearch")?.value||"").trim().toLowerCase();let filtered=docs;
+    if(q){const hits=await window.JaneOfflineKnowledge.searchKnowledge(q,100);const ids=new Set(hits.map(h=>h.chunk.docId));filtered=docs.filter(d=>String(d.name||"").toLowerCase().includes(q)||ids.has(d.id));}
+    if(!filtered.length){list.innerHTML=`<div class="v78-kb-empty">${docs.length?"No Archives matched that search.":"No knowledge has been uploaded yet. Tap Import Knowledge to teach Jane."}</div>`;return;}
+    list.innerHTML=filtered.map(d=>`<article class="v78-archive-card" data-doc-id="${escapeHtml(d.id)}"><div class="v78-archive-icon">${iconFor(d.mime,d.name)}</div><div><div class="v78-archive-title">${escapeHtml(d.name)}</div><div class="v78-archive-meta">${escapeHtml(d.mime||"Unknown type")} · ${humanBytes(d.meta?.fileBytes)} · ${Number(d.chunks||0)} sections · ${dateText(d.importedAt)}</div></div><div class="v78-archive-buttons"><button class="v78-kb-btn secondary v78-open-entry" type="button">Open</button><button class="v78-kb-btn danger v78-delete-entry" type="button">Delete</button></div></article>`).join("");
+  }
+  async function openEntry(id){const result=await window.JaneOfflineKnowledge.getDocument(id);if(!result)return;byId("v78ReaderTitle").textContent=result.doc.name;byId("v78ReaderMeta").textContent=(result.doc.mime||"Unknown type")+" · "+Number(result.doc.chunks||0)+" sections · imported "+dateText(result.doc.importedAt);byId("v78ReaderText").textContent=result.text||"No extracted text is available for this archive entry.";byId("janeKnowledgeBase")?.classList.add("v78-reader-open");byId("v78ReaderText")?.scrollTo(0,0);}
+  async function deleteEntry(id){const doc=docs.find(d=>d.id===id);if(!doc)return;if(!confirm(`Delete “${doc.name}” from Jane’s Knowledge Base?`))return;await window.JaneOfflineKnowledge.deleteDocument(id);showBrowser();await refresh();}
+  function importFiles(){if(window.AndroidJane&&typeof window.AndroidJane.pickKnowledgeFile==="function")window.AndroidJane.pickKnowledgeFile();else byId("v77ImportKnowledgeBtn")?.click();}
+  byId("v78CloseKnowledgeBtn")?.addEventListener("click",closeScreen);byId("v78ImportBtn")?.addEventListener("click",importFiles);byId("v78RefreshArchives")?.addEventListener("click",refresh);byId("v78ReaderBack")?.addEventListener("click",showBrowser);byId("v78ArchiveSearch")?.addEventListener("input",()=>render());
+  byId("v78ArchiveList")?.addEventListener("click",e=>{const card=e.target.closest("[data-doc-id]");if(!card)return;if(e.target.closest(".v78-delete-entry"))deleteEntry(card.dataset.docId);else if(e.target.closest(".v78-open-entry")||!e.target.closest("button"))openEntry(card.dataset.docId);});
+  document.addEventListener("jane-knowledge-changed",refresh);
+  function addSettingsButton(){const panel=byId("v77OfflineKnowledgePanel");if(!panel||byId("v78OpenKnowledgeBaseBtn"))return;const actions=panel.querySelector(".settings-actions")||panel;const btn=document.createElement("button");btn.id="v78OpenKnowledgeBaseBtn";btn.className="primary";btn.type="button";btn.textContent="Open Knowledge Base / Archives";btn.addEventListener("click",e=>{e.preventDefault();openScreen();});actions.prepend(btn);}
+  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",()=>{addSettingsButton();setTimeout(addSettingsButton,500);});else addSettingsButton();setTimeout(addSettingsButton,500);setTimeout(addSettingsButton,1500);
+  window.JaneOpenKnowledgeBase=openScreen;
+})();
+</script>
+<!-- V78: dedicated Knowledge Base and Archives screen; strict knowledge-bounded answers; browse/open/search/delete uploaded entries. -->
+
 </body>
 </html>
--- v78work/app/src/main/java/com/example/janeai/MainActivity.java	2026-08-02 20:49:28.000000000 +0000
+++ v78mod/app/src/main/java/com/example/janeai/MainActivity.java	2026-08-05 00:25:13.710685415 +0000
@@ -281,6 +281,16 @@
         return type == null || type.trim().isEmpty() ? "application/octet-stream" : type;
     }
 
+    private long getFileSize(Uri uri) {
+        try (Cursor cursor = getContentResolver().query(uri, new String[]{OpenableColumns.SIZE}, null, null, null)) {
+            if (cursor != null && cursor.moveToFirst()) {
+                int sizeIndex = cursor.getColumnIndex(OpenableColumns.SIZE);
+                if (sizeIndex >= 0 && !cursor.isNull(sizeIndex)) return cursor.getLong(sizeIndex);
+            }
+        } catch (Exception ignored) {}
+        return 0L;
+    }
+
 
     // V76: dedicated multi-file offline knowledge picker for PDFs, documents, text, and photos.
     private void openJaneFilePicker() {
@@ -438,6 +448,7 @@
         String name = getFileName(uri);
         String mimeType = getMimeType(uri);
         String importId = "knowledge_" + System.currentTimeMillis() + "_" + fileIndex;
+        long fileBytes = getFileSize(uri);
         notifyKnowledgeJs("JaneKnowledgeImportStart", importId, name, mimeType, String.valueOf(totalFiles));
         try {
             String text = extractKnowledgeText(uri, name, mimeType, importId);
@@ -445,7 +456,7 @@
-            String meta = "{\"source\":\"native-offline-ocr\",\"fileIndex\":" + fileIndex + ",\"totalFiles\":" + totalFiles + "}";
+            String meta = "{\"source\":\"native-offline-ocr\",\"fileIndex\":" + fileIndex + ",\"totalFiles\":" + totalFiles + ",\"fileBytes\":" + fileBytes + "}";
             sendKnowledgeTextToWeb(importId, name, mimeType, text, meta);
         } catch (Exception error) {
             notifyKnowledgeJs("JaneKnowledgeImportError", importId, name, error.getMessage() == null ? "Import failed." : error.getMessage());
