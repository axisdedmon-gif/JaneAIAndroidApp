plugins {
    id("com.android.application")
}

android {
    signingConfigs {
        janeStable {
            storeFile file("jane-update-key.jks")
            storePassword "JaneUpdate2026"
            keyAlias "janeupdate"
            keyPassword "JaneUpdate2026"
        }
    }
    androidResources {
        noCompress += ["task"]
    }

    namespace = "com.example.janeai"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.example.janeai"
        minSdk = 24
        targetSdk = 35
        versionCode = 200001
        versionName = "Beta 1.0.01"
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.janeStable
        }
    }
}


dependencies {
    implementation("com.google.mediapipe:tasks-genai:0.10.27")

    implementation("com.google.mlkit:text-recognition:16.0.1")
    implementation("com.tom-roush:pdfbox-android:2.0.27.0")
}
