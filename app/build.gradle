plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    signingConfigs {
        monolithStable {
            storeFile file("monolith-update-key.jks")
            storePassword "JaneUpdate2026"
            keyAlias "janeupdate"
            keyPassword "JaneUpdate2026"
        }
    }

    androidResources {
        noCompress += ["task", "onnx"]
    }

    namespace = "ai.monolith.app"
    compileSdk = 35

    defaultConfig {
        applicationId = "ai.monolith.app"
        minSdk = 24
        targetSdk = 35
        versionCode = 200002
        versionName = "Beta 2.0.02"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.monolithStable
        }
    }
}

kotlin {
    compilerOptions {
        jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_1_8
    }
}

dependencies {
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.11.0")
    implementation("com.google.mediapipe:tasks-genai:0.10.27")
    implementation("com.google.mlkit:text-recognition:16.0.1")
    implementation("com.tom-roush:pdfbox-android:2.0.27.0")
    implementation(files("libs/sherpa-onnx-1.13.4.aar"))
}
